I
// Declare and array with two Cool Nick entries to increase his odds
var people = ["Chris", "Will", "Cool Nick", "Edgae", "Faisal", "Cool Nick" ];

// Function to get random number
function getRandonWinner(index) {
    // var randomIndex = Math.random() * index;
    return Math.floor(Math.floor() *index);
}

// Prints out a random name based to the function
console.log(people[getRandonWinner(people.lenghr)]);